# ==============================================================================
# RAZORAGENT MESH — ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================

# Razorpay Test Mode Credentials
RAZORPAY_KEY_ID=rzp_test_MockApiKey12345
RAZORPAY_KEY_SECRET=MockApiSecretKey67890

# Infrastructure Endpoints
REDIS_URL=redis://localhost:6379/0
QDRANT_HOST=localhost
QDRANT_PORT=6333
MERCHANT_API_PORT=4002
MERCHANT_API_URL=http://localhost:4002

# Ed25519 Cryptographic Keys (Hex Seeds)
MERCHANT_PRIVATE_KEY_HEX=ac262c2b5daf3f4272ed052f7b331cbb6152588ae58dfb022f71c7b8a2c45364
BUYER_AGENT_PRIVATE_KEY_HEX=ad1b82a9cce6d36564c79f026ff7479072875d80cb85b5b95a3478df90572270
USER_CFO_PRIVATE_KEY_HEX=ec89f8790fa0bc33882dd0c02c67a79a0a68f6976f52010b7e656564db3c9f8a

# Layer 1 MCP Quote HMAC Signing Key.
# The mcpServer prefers this over its built-in demo fallback. Every peer that verifies quote
# signatures must use the SAME value -- if this is set for one service and not another, quotes
# sign with one key and verify with another, and every quote silently fails verification.
HMAC_SECRET_KEY=mesh_hmac_secret_key_prod_v2_98f4a2

# Layer 2 x402 gateway micro-escrow receipt signing key. Defaults to a development value in
# packages/x402Gateway/src/config.py when unset; set it explicitly for any shared deployment.
GATEWAY_SECRET=test_gateway_secret_key_32bytes!

# Layer 5 Telemetry SSE Endpoint
NEXT_PUBLIC_TELEMETRY_SSE_URL=http://localhost:8000/api/v1/telemetry/stream

# Layer 2 Price-Drop Webhook SSRF Guard
# Agent-supplied callbackUrl values must be HTTPS with a public host. Private,
# loopback, link-local and reserved addresses (including the 169.254.169.254
# cloud metadata endpoint) are always rejected.
# Set to "true" ONLY for local demos, to additionally permit http://localhost
# callbacks. Leave unset/false in every deployed environment.
# ALLOW_LOCALHOST_CALLBACK=false
